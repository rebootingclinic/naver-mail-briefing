<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>CH-AIDE 메일 브리핑</title>
  <link rel="stylesheet" href="/style.css" />
</head>
<body>
  <header>
    <div class="header-inner">
      <div>
        <h1>📋 CH-AIDE 브리핑</h1>
        <p class="subtitle">ch-aide@aidepartners.com 수신 PDF 자동 브리핑</p>
      </div>
      <div class="header-actions">
        <% if (kakaoConnected) { %>
          <span class="kakao-badge">💬 카카오 연결됨</span>
        <% } else { %>
          <a href="/auth/kakao" class="btn-kakao">💬 카카오 알림 연결</a>
        <% } %>
        <form method="POST" action="/check" style="display:inline">
          <button type="submit" class="btn-check">🔄 지금 확인</button>
        </form>
      </div>
    </div>
  </header>

  <main>
    <% if (query.kakao === 'success') { %>
      <div class="toast toast-kakao">💬 카카오톡 알림이 연결됐어요! 새 브리핑이 오면 카카오톡으로 알려드립니다.</div>
    <% } else if (query.kakao === 'error') { %>
      <div class="toast toast-error">❌ 카카오 연결에 실패했어요. 다시 시도해주세요.</div>
    <% } %>

    <% if (query.checked) { %>
      <div class="toast">
        <% if (parseInt(query.new) > 0) { %>
          ✅ 새 브리핑 <strong><%= query.new %>건</strong>이 추가됐어요!
        <% } else { %>
          ✅ 확인 완료 — 새 메일이 없어요.
        <% } %>
      </div>
    <% } %>

    <% if (briefings.length === 0) { %>
      <div class="empty">
        <p>📭 아직 브리핑이 없어요.</p>
        <p>ch-aide@aidepartners.com 에서 PDF가 포함된 메일이 오면 자동으로 표시됩니다.</p>
      </div>
    <% } else { %>
      <div class="grid">
        <% briefings.forEach(b => { %>
          <a href="/briefing/<%= b.id %>" class="card">
            <div class="card-date"><%= b.mail_date %></div>
            <h2 class="card-title"><%= b.subject %></h2>
            <div class="card-file">📄 <%= b.pdf_filename %></div>
            <p class="card-preview"><%= b.preview %>...</p>
            <span class="card-link">자세히 보기 →</span>
          </a>
        <% }) %>
      </div>
    <% } %>
  </main>

  <footer>
    <p>1시간마다 자동으로 메일을 확인합니다.</p>
  </footer>
</body>
</html>
